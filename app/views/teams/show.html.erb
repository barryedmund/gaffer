<p id="notice"><%= notice %></p>

<span class="main_header">
	<%= link_to @team.league.name, league_path(@team.league) %> >> <%= @team.title %> (<%= link_to 'edit', edit_league_team_path(@team.league, @team) %>)
</span>

<%= render partial: "shared/team_players" %>
