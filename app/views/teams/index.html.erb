<h1>Listing teams</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr id="<%= dom_id(team) %>">
        <td><%= team.title %></td>
        <td>
          <%= link_to "View team players", team_team_players_path(team) %>
          <%= link_to 'Show', team %>
          <%= link_to 'Edit', edit_team_path(team) %>
          <%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Team', new_team_path %>
