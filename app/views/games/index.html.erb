<h1>Listing games</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= link_to game.match_up, game_path(game) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
